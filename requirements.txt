# Python 3.12 or newer. numpy 2.5 is the binding constraint - it publishes no wheels
# for 3.11 or below, so these exact pins cannot be installed there.
# Verified by CI on 3.12 and 3.13.
faiss-cpu==1.15.0
google-genai==2.20.0
numpy==2.5.2
pandas==3.0.5
python-dotenv==1.2.3
streamlit==1.62.0
