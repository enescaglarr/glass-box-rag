[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
# The project root, so `from src.helper import ...` resolves. Without this, a bare
# `pytest` fails while `python -m pytest` succeeds - the latter puts the working
# directory on sys.path, the former does not, and CI runs the bare form.
pythonpath = .
